<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Key_Account_Formula__c</fullName>
    <description>derived from key account on the parent account</description>
    <externalId>false</externalId>
    <formula>IF( NOT(ISBLANK(ParentId)), Parent.Key_Account__c, false)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Derived from the Key Account field from the parent</inlineHelpText>
    <label>Key Account</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
